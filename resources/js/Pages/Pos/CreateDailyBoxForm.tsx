import {TodayBox} from "@/Pages/Pos/MainContentPos.tsx";

export default function CreateDailyBoxForm({onDailyBoxCreated}: {
  onDailyBoxCreated: (todayBox: TodayBox) => void;
}) {
  return (
    <></>
  )
}
